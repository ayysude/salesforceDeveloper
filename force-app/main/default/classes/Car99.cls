public class Car99 {
    //public classes are accesible only within the package. restrictive (appExchange app) can be used within application
    //global classes are accesible outside of the package also. more open than public. can be referanced outside of  the application
    public void switchOn() {
        sydtem.debug('starting the car');

    }
}
